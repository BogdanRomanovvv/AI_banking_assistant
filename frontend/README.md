# AI Banking Assistant - Frontend

Современный веб-интерфейс для AI Banking Assistant с дизайном в стиле Jira. Построен на React + TypeScript с компонентной архитектурой и drag & drop функционалом.

## 🎨 Дизайн и Цветовая Палитра

**Цветовая схема:** Сине-бело-оранжевая

- **Primary Dark Blue:** `#0B4F8C` - Основной тёмно-синий
- **Primary Blue:** `#1E6FD9` - Основной синий (brand)
- **Blue Light:** `#E9F2FF` - Светло-синий фон
- **White:** `#FFFFFF` - Белый
- **Accent Orange:** `#FF6A00` - Оранжевый (кнопки/акценты)
- **Orange Light:** `#FF8A33` - Светло-оранжевый (hover/метки)
- **Neutral Gray:** `#6B7280` - Серый (текст)

## 📦 Структура Проекта

```
frontend/
├── src/
│   ├── components/
│   │   ├── board/              # Kanban доска
│   │   │   ├── Board.tsx       # Контейнер доски с DnD
│   │   │   ├── Column.tsx      # Колонка (TO DO, IN PROGRESS, etc.)
│   │   │   ├── Card.tsx        # Карточка письма
│   │   │   └── *.module.css    # CSS модули
│   │   ├── layout/             # Лейаут компоненты
│   │   │   ├── Header.tsx      # Верхняя панель
│   │   │   └── *.module.css
│   │   ├── ui/                 # Базовые UI компоненты
│   │   │   ├── Button.tsx      # Кнопка
│   │   │   ├── Input.tsx       # Поле ввода
│   │   │   ├── Modal.tsx       # Модальное окно
│   │   │   ├── Avatar.tsx      # Аватар пользователя
│   │   │   ├── Chip.tsx        # Метка/тег
│   │   │   └── *.module.css
│   │   ├── LetterDetail.tsx    # Детали письма
│   │   ├── LoginForm.tsx       # Форма входа
│   │   └── UserManagement.tsx  # Управление пользователями
│   ├── services/
│   │   └── api.ts              # API сервисы
│   ├── styles/
│   │   ├── tokens.ts           # Design tokens (цвета, spacing)
│   │   └── global.css          # Глобальные стили
│   ├── types/
│   │   └── index.ts            # TypeScript типы
│   ├── App.tsx                 # Главный компонент
│   ├── App.css                 # Стили приложения
│   └── main.tsx                # Точка входа
├── package.json
├── vite.config.ts
└── tsconfig.json
```

## 🚀 Быстрый Старт

### Предварительные требования

- **Node.js** >= 16.x
- **npm** >= 8.x

### Установка зависимостей

```powershell
cd frontend
npm install
```

### Запуск в режиме разработки

```powershell
npm run dev
```

Приложение откроется по адресу: http://localhost:5173

### Сборка для продакшена

```powershell
npm run build
```

Собранные файлы окажутся в папке `dist/`.

### Предварительный просмотр production-сборки

```powershell
npm run preview
```

## 🎯 Основные Возможности

### Kanban Доска
- ✅ 4 колонки статусов: TO DO, IN PROGRESS, IN REVIEW, DONE
- ✅ Drag & Drop перетаскивание карточек между колонками
- ✅ Автоматическое обновление каждые 30 секунд
- ✅ Цветные метки по типу письма
- ✅ Приоритеты (Высокий/Средний/Низкий)
- ✅ Аватары исполнителей

### Карточки Писем
- Номер письма (MBA-X)
- Тема письма
- Краткое описание из анализа
- Тип письма (chip)
- Приоритет (chip)
- Аватар исполнителя
- Дата создания

### UI Компоненты
- **Button:** 4 варианта (primary, secondary, ghost, danger)
- **Input:** С лейблами, ошибками, иконками
- **Modal:** Всплывающее окно с backdrop
- **Avatar:** Генерация инициалов и цветов
- **Chip:** Цветные метки (6 цветов)

### Header
- Логотип и название приложения
- Навигация (Доска / Пользователи)
- Информация о текущем пользователе
- Кнопка выхода

## 🛠️ Технологический Стек

- **React** 18.2 - UI библиотека
- **TypeScript** 5.2 - Типизация
- **Vite** 5.0 - Сборщик и dev-сервер
- **@hello-pangea/dnd** 16.5 - Drag & Drop (форк react-beautiful-dnd)
- **date-fns** 2.30 - Работа с датами
- **axios** 1.6 - HTTP клиент
- **CSS Modules** - Изолированные стили
- **lucide-react** - Иконки (опционально)

## 🎨 Design Tokens

Все токены дизайна находятся в `src/styles/tokens.ts`:

```typescript
// Цвета
colors.primary       // #1E6FD9
colors.accent        // #FF6A00
colors.white         // #FFFFFF

// Отступы
spacing.sm   // 8px
spacing.md   // 12px
spacing.lg   // 16px

// Радиусы
borderRadius.md  // 8px

// Тени
shadows.md  // box-shadow для карточек
```

## 📝 Переменные Окружения

Создайте файл `.env` в корне `frontend/`:

```env
VITE_API_URL=http://localhost:8000
```

## 🐳 Docker

Проект уже настроен для работы в Docker. Смотрите `docker-compose.yml` в корне репозитория.

```powershell
# Запуск всего стека (backend + frontend)
docker-compose up -d

# Остановка
docker-compose down
```

Frontend будет доступен на http://localhost:3000

## 🧪 Тестирование

*(Опционально - можно добавить позже)*

```powershell
# Установка зависимостей для тестов
npm install -D @testing-library/react @testing-library/jest-dom vitest

# Запуск тестов
npm run test
```

## 📚 Использование Компонентов

### Пример использования Board

```tsx
import { Board } from './components/board';

<Board
  letters={letters}
  onCardClick={(letter) => console.log(letter)}
  onStatusChange={(id, status) => updateStatus(id, status)}
/>
```

### Пример использования Button

```tsx
import { Button } from './components/ui';

<Button variant="primary" size="md" onClick={handleClick}>
  Сохранить
</Button>
```

### Пример использования Modal

```tsx
import { Modal } from './components/ui';

<Modal
  isOpen={isOpen}
  onClose={() => setIsOpen(false)}
  title="Заголовок"
  size="lg"
>
  Содержимое модального окна
</Modal>
```

## 🔧 Кастомизация

### Изменение цветовой палитры

Отредактируйте `src/styles/tokens.ts`:

```typescript
export const colors = {
  primary: '#ВАШ_ЦВЕТ',
  accent: '#ВАШ_ОРАНЖЕВЫЙ',
  // ...
};
```

### Добавление новых статусов колонок

В `src/styles/tokens.ts` добавьте в `statusColumns`:

```typescript
export const statusColumns = {
  // ...
  YOUR_STATUS: {
    id: 'YOUR_STATUS',
    title: 'Ваш статус',
    color: colors.primary,
    bgColor: colors.primaryLight,
  },
};
```

## 🐛 Решение Проблем

### npm не найден

Установите Node.js: https://nodejs.org/

### Ошибки TypeScript после клонирования

```powershell
npm install
```

### Порт 5173 занят

Измените порт в `vite.config.ts`:

```typescript
export default defineConfig({
  server: {
    port: 3001,
  },
});
```

### CORS ошибки

Убедитесь, что backend настроен с правильными CORS headers. В `backend/app/main.py`:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

## 📖 Документация API

Backend API документация доступна по адресу:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## 🤝 Участие в Разработке

1. Создайте ветку для новой фичи: `git checkout -b feature/your-feature`
2. Коммитьте изменения: `git commit -m 'Add some feature'`
3. Пушьте в ветку: `git push origin feature/your-feature`
4. Создайте Pull Request

## 📄 Лицензия

MIT License

## 👥 Авторы

Разработано командой AI Banking Assistant

---

**Удачи с разработкой! 🚀**
