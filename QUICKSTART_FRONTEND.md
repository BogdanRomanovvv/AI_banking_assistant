# 🚀 Быстрый Старт - AI Banking Assistant Frontend

## Команды для PowerShell (Windows)

### 1. Установка зависимостей

```powershell
cd c:\MY_DIRECTORY\STUDING_MAI\3_sem\test\AI_banking_assistant\frontend
npm install
```

Установит все необходимые пакеты, включая:
- React + TypeScript
- @hello-pangea/dnd (drag & drop)
- date-fns
- axios
- lucide-react (иконки)

### 2. Запуск dev-сервера

```powershell
npm run dev
```

Откроется по адресу: **http://localhost:5173**

### 3. Сборка для продакшена

```powershell
npm run build
```

Готовые файлы будут в папке `dist/`

---

## 🎨 Что Создано

### ✅ Design System
- **Токены цветов** (`src/styles/tokens.ts`) - сине-бело-оранжевая палитра
- **Глобальные стили** (`src/styles/global.css`) - типографика, scrollbar, reset

### ✅ UI Компоненты (`src/components/ui/`)
- `Button` - 4 варианта (primary, secondary, ghost, danger)
- `Input` - с лейблами и валидацией
- `Modal` - всплывающие окна
- `Avatar` - генерация инициалов и цветов
- `Chip` - цветные метки/теги

### ✅ Board Компоненты (`src/components/board/`)
- `Board` - главный контейнер с DragDropContext
- `Column` - колонка (TO DO, IN PROGRESS, IN REVIEW, DONE)
- `Card` - карточка письма с drag & drop

### ✅ Layout (`src/components/layout/`)
- `Header` - верхняя панель с навигацией, логотипом, пользователем

### ✅ Обновлённый App.tsx
- Интеграция новых компонентов
- Использование Board вместо старого KanbanBoard
- Модальное окно на базе нового Modal компонента

---

## 📁 Структура Файлов

```
frontend/src/
├── components/
│   ├── board/          # Kanban-доска с DnD
│   ├── layout/         # Header и лейауты
│   └── ui/             # Базовые UI компоненты
├── styles/
│   ├── tokens.ts       # Design tokens
│   └── global.css      # Глобальные стили
├── App.tsx             # Обновлён под новую архитектуру
└── main.tsx            # Подключён global.css
```

---

## 🎯 Основные Фичи

- ✅ **Drag & Drop** между колонками
- ✅ **Цветовая схема** сине-бело-оранжевая
- ✅ **TypeScript** типизация
- ✅ **CSS Modules** для изоляции стилей
- ✅ **Адаптивность** (desktop/tablet/mobile)
- ✅ **Токены дизайна** для единообразия
- ✅ **Компонентная архитектура**

---

## 🐛 Возможные Проблемы

### Если npm не найден
Установите Node.js: https://nodejs.org/

### Ошибки после установки
```powershell
rm -r node_modules
rm package-lock.json
npm install
```

### Порт занят
Измените в `vite.config.ts`:
```typescript
server: { port: 3001 }
```

---

## 📖 Документация

Полная документация: `frontend/README.md`

API документация backend:
- Swagger: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

---

**Готово! Можно запускать `npm install` и `npm run dev` 🎉**
