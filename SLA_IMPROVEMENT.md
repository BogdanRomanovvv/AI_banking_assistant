# Улучшение системы определения SLA

## 📅 Дата: 29 ноября 2025 г.

## 🎯 Что сделано

Добавлены **детальные правила определения SLA** для более точного и обоснованного расчёта времени ответа на письма.

## 📋 Правила определения SLA

### 1. **СРОЧНЫЕ - 2 часа**
- Регуляторные запросы с пометкой "срочно"
- Жалобы с угрозой обращения в суд/СМИ
- Блокировки счетов/карт клиентов
- Технические сбои, влияющие на работу клиентов

### 2. **ВЫСОКИЙ ПРИОРИТЕТ - 4 часа**
- Запросы от государственных органов (ЦБ РФ, налоговая, полиция)
- Жалобы на качество обслуживания
- Запросы от корпоративных клиентов
- Запросы от СМИ

### 3. **СТАНДАРТНЫЙ - 24 часа (1 рабочий день)**
- Типовые информационные запросы
- Вопросы о продуктах/тарифах
- Запросы справок/выписок
- Партнерские предложения

### 4. **ОБЫЧНЫЙ - 72 часа (3 рабочих дня)**
- Коммерческие предложения
- Маркетинговые исследования
- Запросы на сотрудничество

### 5. **УВЕДОМЛЕНИЯ - 0 часов**
- Письма с пометкой "ответ не требуется"
- Информационные уведомления
- Подтверждения получения документов

## 🔍 Новое поле: `sla_reasoning`

AI теперь **объясняет**, почему выбран именно этот SLA:

```json
{
  "sla_hours": 4,
  "sla_reasoning": "Запрос от государственного органа требует оперативного ответа в течение 4 часов"
}
```

### Где отображается:
- В карточке письма (LetterDetail) под badge SLA
- В API ответах (`/api/letters/{id}`)
- В базе данных (таблица `letters`, поле `sla_reasoning`)

## 🛠 Технические изменения

### Backend:
1. **yandex_gpt.py** - добавлены детальные правила в system prompt
2. **models.py** - добавлено поле `sla_reasoning: Column(Text, nullable=True)`
3. **schemas.py** - добавлено поле в `LetterResponse` и `AnalysisResponse`
4. **letter_service.py** - сохранение `sla_reasoning` из анализа
5. **add_sla_reasoning.sql** - SQL миграция для добавления колонки

### Frontend:
1. **types/index.ts** - добавлено поле `sla_reasoning?: string` в интерфейс `Letter`
2. **LetterDetail.tsx** - отображение объяснения SLA под badge (💡 emoji + курсив)

### Database:
```sql
ALTER TABLE letters 
ADD COLUMN IF NOT EXISTS sla_reasoning TEXT;
```

## 📊 Примеры работы

### Пример 1: Жалоба с угрозой суда
```
SLA: 2 часа
💡 Жалоба содержит угрозу судебного разбирательства, требуется срочный ответ
```

### Пример 2: Информационный запрос
```
SLA: 24 часа
💡 Стандартный информационный запрос о тарифах обрабатывается в течение рабочего дня
```

### Пример 3: Коммерческое предложение
```
SLA: 72 часа
💡 Коммерческое предложение о партнерстве не требует срочного ответа
```

## ✅ Преимущества

1. **Прозрачность** - сотрудники видят, почему установлен именно такой SLA
2. **Обоснованность** - AI следует чётким правилам, а не произвольно выбирает значение
3. **Контроль** - можно проверить логику AI и скорректировать при необходимости
4. **Учёт контекста** - AI учитывает явные указания в письме ("просим ответить в течение 48 часов")
5. **Аналитика** - можно анализировать распределение писем по категориям срочности

## 🔄 Обратная совместимость

- Старые письма без `sla_reasoning` продолжают работать (поле nullable)
- Fallback значение: "Стандартный SLA применен из-за ошибки анализа" (при ошибках парсинга)
- По умолчанию: 24 часа, если AI не смог определить SLA

## 📝 Следующие шаги

- [ ] Собрать статистику по точности определения SLA
- [ ] Добавить возможность вручную корректировать SLA с обоснованием
- [ ] Отображать SLA в списке писем (не только в детальном виде)
- [ ] Добавить уведомления за X часов до истечения SLA
