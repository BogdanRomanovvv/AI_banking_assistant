# 🎨 Визуальный Гайд по Компонентам

## Цветовая Палитра

```
🔵 Primary Dark Blue:  #0B4F8C  (тёмно-синий, заголовки)
🔵 Primary Blue:       #1E6FD9  (основной синий, ссылки)
🔵 Blue Light:         #E9F2FF  (светлый фон, hover)
⚪ White:              #FFFFFF  (фон карточек)
🟠 Accent Orange:      #FF6A00  (кнопки, акценты)
🟠 Orange Light:       #FF8A33  (hover кнопок, метки)
⚫ Neutral Gray:       #6B7280  (текст, второстепенный)
```

---

## 🧩 UI Компоненты

### Button (Кнопка)

**Использование:**
```tsx
import { Button } from './components/ui';

<Button variant="primary" size="md" onClick={handleClick}>
  Сохранить
</Button>
```

**Варианты:**
- `primary` - оранжевая (основные действия)
- `secondary` - синяя обводка (второстепенные)
- `ghost` - прозрачная (неважные)
- `danger` - красная (удаление)

**Размеры:**
- `sm` - 32px высота
- `md` - 40px (по умолчанию)
- `lg` - 48px

**Props:**
- `loading?: boolean` - показывает спиннер
- `icon?: ReactNode` - иконка слева
- `fullWidth?: boolean` - на всю ширину

---

### Input (Поле ввода)

**Использование:**
```tsx
import { Input } from './components/ui';

<Input
  label="Email"
  placeholder="Введите email"
  error="Неверный формат"
  fullWidth
/>
```

**Props:**
- `label?: string` - текст над полем
- `error?: string` - текст ошибки (красная обводка)
- `helperText?: string` - подсказка под полем
- `icon?: ReactNode` - иконка слева внутри
- `fullWidth?: boolean` - на всю ширину

---

### Modal (Модальное окно)

**Использование:**
```tsx
import { Modal, Button } from './components/ui';

<Modal
  isOpen={isOpen}
  onClose={() => setIsOpen(false)}
  title="Заголовок окна"
  size="lg"
  footer={
    <>
      <Button variant="ghost" onClick={() => setIsOpen(false)}>
        Отмена
      </Button>
      <Button variant="primary" onClick={handleSave}>
        Сохранить
      </Button>
    </>
  }
>
  Содержимое модального окна
</Modal>
```

**Размеры:**
- `sm` - 400px
- `md` - 600px (по умолчанию)
- `lg` - 800px
- `xl` - 1000px

**Особенности:**
- Закрывается по Escape
- Блокирует прокрутку body
- Backdrop можно кликнуть для закрытия

---

### Avatar (Аватар)

**Использование:**
```tsx
import { Avatar } from './components/ui';

<Avatar name="Иван Иванов" size="md" />
<Avatar name="John Doe" src="/path/to/image.jpg" size="lg" />
```

**Размеры:**
- `sm` - 24px (в карточках)
- `md` - 32px (по умолчанию)
- `lg` - 48px (в профиле)

**Особенности:**
- Автоматически генерирует инициалы
- Цвет фона зависит от имени (детерминированно)
- 6 цветов: оранжевый, синий, зелёный, жёлтый, фиолетовый, розовый

---

### Chip (Метка/Тег)

**Использование:**
```tsx
import { Chip } from './components/ui';

<Chip label="Высокий приоритет" color="orange" size="sm" />
<Chip label="Complaint" color="blue" onRemove={() => console.log('remove')} />
```

**Цвета:**
- `blue` - синий (info_request)
- `orange` - оранжевый (complaint, высокий приоритет)
- `green` - зелёный (partnership)
- `yellow` - жёлтый (approval_request, средний)
- `gray` - серый (notification, низкий)
- `purple` - фиолетовый (regulatory)

**Размеры:**
- `sm` - 20px высота (по умолчанию, в карточках)
- `md` - 24px

**Props:**
- `onRemove?: () => void` - показывает крестик для удаления

---

## 🎴 Board Компоненты

### Card (Карточка письма)

**Структура:**
```
┌──────────────────────────────┐
│ MBA-5         [Высокий]      │ ← header (ID + приоритет)
│                              │
│ Тема письма (заголовок)      │ ← title
│                              │
│ [Жалоба]                     │ ← type chip
│                              │
│ Краткое описание из          │ ← summary (2 строки макс)
│ анализа письма...            │
│                              │
├──────────────────────────────┤
│ 👤 ИИ         12 Nov         │ ← footer (аватар + дата)
└──────────────────────────────┘
```

**Drag & Drop:**
- Плавная анимация при перетаскивании
- Поворот на 2° при захвате
- Тень увеличивается

---

### Column (Колонка)

**Статусы:**
```
TO DO          IN PROGRESS     IN REVIEW       DONE
(NEW)          (ANALYZING)     (DRAFT_READY)   (APPROVED)
Серый          Синий           Жёлтый          Зелёный
```

**Особенности:**
- Счётчик карточек в заголовке
- Кнопка "+" только в TO DO
- Droppable зона подсвечивается при hover
- Скролл внутри колонки

---

### Board (Доска)

**Layout:**
```
┌────────────────────────────────────────────────────┐
│  [TO DO] [IN PROGRESS] [IN REVIEW] [DONE]         │
│    (3)       (5)          (2)        (8)          │
│  ┌────┐   ┌────┐      ┌────┐     ┌────┐          │
│  │Card│   │Card│      │Card│     │Card│          │
│  └────┘   └────┘      └────┘     └────┘          │
│  ┌────┐   ┌────┐      ┌────┐     ┌────┐          │
│  │Card│   │Card│      │    │     │Card│          │
│  └────┘   └────┘      └────┘     └────┘          │
│   ...      ...          ...        ...            │
└────────────────────────────────────────────────────┘
```

**Горизонтальный скролл** если колонки не влезают

---

## 🧱 Layout

### Header (Шапка)

**Структура:**
```
┌──────────────────────────────────────────────────────┐
│ 🏦 AI Banking Assistant  [Доска] [Пользователи]     │
│                                 Иван Иванов  [Выход]│
│                                 admin                │
└──────────────────────────────────────────────────────┘
```

**Особенности:**
- Логотип с эмодзи 🏦
- Навигация с подчёркиванием оранжевым активной вкладки
- Информация о пользователе (имя + роль)
- Кнопка выхода (ghost)

---

## 📱 Адаптивность

### Desktop (> 1024px)
- 4 колонки рядом
- Широкий header
- Полные имена и тексты

### Tablet (768px - 1024px)
- 3-4 колонки (горизонтальный скролл)
- Компактный header
- Сокращённые тексты

### Mobile (< 768px)
- 1-2 колонки (горизонтальный скролл обязателен)
- Header в 2 строки (лого + навигация, юзер + выход)
- Chip и аватары меньше

---

## 🎭 Анимации

### Hover эффекты:
- Карточки: `transform: translateY(-2px)` + тень
- Кнопки: `transform: translateY(-1px)` + тень
- Chip: opacity на крестике

### Drag & Drop:
- Захват: `rotate(2deg)` + увеличенная тень
- Hover на droppable: светло-синий фон колонки

### Модальное окно:
- Backdrop: `fadeIn` 0.2s
- Окно: `slideUp` 0.3s

---

## 🔤 Типографика

**Шрифт:** Inter (Google Fonts)

**Размеры:**
- `12px` - xs (метки, даты)
- `14px` - sm (основной текст, кнопки)
- `16px` - base (заголовки карточек)
- `18px` - lg (H3)
- `20px` - xl (H2)
- `24px` - 2xl (H1, логотип)

**Веса:**
- `400` - normal (текст)
- `500` - medium (кнопки, метки)
- `600` - semibold (заголовки)
- `700` - bold (логотип)

---

## 📏 Spacing (отступы)

```
xs:   4px   (внутренние в Chip)
sm:   8px   (между элементами)
md:   12px  (padding карточек)
lg:   16px  (padding колонок)
xl:   24px  (padding header)
xxl:  32px  (большие разрывы)
```

---

## 🎨 Тени

```
sm:   0 1px 2px rgba(16,24,40,0.06)    (карточки)
md:   0 4px 6px rgba(16,24,40,0.06)    (hover карточек)
lg:   0 10px 15px rgba(16,24,40,0.10)  (dragging карточки)
xl:   0 20px 25px rgba(16,24,40,0.10)  (модальное окно)
```

---

## ✅ Лучшие Практики

1. **Всегда используйте токены** из `src/styles/tokens.ts`
2. **CSS Modules** для изоляции стилей компонентов
3. **TypeScript интерфейсы** для всех props
4. **Accessibility:** aria-label, keyboard navigation
5. **Адаптивность:** используйте media queries
6. **Консистентность:** одинаковые размеры/отступы везде

---

**Удачи! 🚀**
